################################################################################
# ============================================================================
# Production Environment Configuration - v0.7
# ============================================================================
# 
# Target: Synology NAS (via Tailscale)
# Purpose: Production backend deployment with pre-compiled executable
# Container Name: setprod-backend
# 
# Network Access:
#   - SSH/Management: via Tailscale IP 100.100.10.1:5522
#   - Backend App: https://set.souchefr.synology.me (public, reverse proxy)
#   - MongoDB: Internal network only (no external access)
################################################################################

# Project Configuration
PROJECT_NAME=set_backend
PROJECT_DIR=set_backend
# Absolute path on Synology NAS (only for data directory)
PROJECT_PATH=/volume1/docker/setprod/backend

# Container Names (standardized with setprod- prefix)
CONTAINER_NAME=setprod-backend
CONTAINER_MONGODB=setprod-mongodb
HOSTNAME=setprod-backend-host

# Network
NETWORK_NAME=prod-network

# Port Mappings
APP_PORT=5646              # Application (proxied by Synology to public URL)

# External URL (configured in Synology Reverse Proxy)
EXTERNAL_URL=https://set.souchefr.synology.me

# NAS Access (via Tailscale)
NAS_TAILSCALE_IP=100.100.10.1
NAS_SSH_PORT=5522
NAS_HOST=souchefr.synology.me

# Database Configuration (PRODUCTION CREDENTIALS - CHANGE THESE!)
DB_NAME=rust_app_db
DB_USER=app_user
DB_PASSWORD=ProdSecure789!ChangeThisPassword
DB_ADMIN_USER=admin
DB_ADMIN_PASSWORD=ProdAdmin789!ChangeThisPassword

# User/Group IDs (Synology)
USER_UID=1026          # Thierry's account on the NAS
USER_GID=100           # Synology "users" group
USERNAME=thierry       # NAS username
GROUPNAME=users

# Logging (INFO level for production)
RUST_LOG=info

# Volumes - Absolute paths on Synology NAS
# Note: No Cargo cache needed - executable is pre-compiled
VOLUME_MONGODB_DATA=/volume1/docker/setprod/mongodb/data
VOLUME_MONGODB_INIT=/volume1/docker/setprod/mongodb/init

# Docker Images (pinned to stable versions)
MONGO_IMAGE=mongo:7.0.15-jammy

# MongoDB Collections
COLLECTION_1=setplayers
COLLECTION_2=setgames
COLLECTION_3=setstats
