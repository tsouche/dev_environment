################################################################################
# Test Environment Configuration - v0.5
# 
# Target: Synology NAS (via Tailscale)
# Purpose: Backend testing with pre-compiled executable
# Container Name: settest-backend
# 
# Network Access:
#   - SSH/Management: via Tailscale IP 100.100.10.1:5522
#   - Backend App: https://settest.souchefr.synology.me (public, reverse proxy)
#   - Mongo Express: http://100.100.10.1:8081 (Tailscale network only)
################################################################################

# Project Configuration
PROJECT_NAME=set_backend
PROJECT_DIR=set_backend
# Absolute path on Synology NAS (only for data directory)
PROJECT_PATH=/volume1/docker/settest/backend

# Container Names (standardized with settest- prefix)
CONTAINER_NAME=settest-backend
CONTAINER_MONGODB=settest-mongodb
CONTAINER_MONGO_EXPRESS=settest-mongo-express
HOSTNAME=settest-backend-host

# Network
NETWORK_NAME=test-network

# Port Mappings
APP_PORT=5645              # Application (proxied by Synology to public URL)
MONGO_EXPRESS_PORT=8081    # Mongo Express (accessible via Tailscale only)

# External URL (configured in Synology Reverse Proxy)
EXTERNAL_URL=https://settest.souchefr.synology.me

# NAS Access (via Tailscale)
NAS_TAILSCALE_IP=100.100.10.1
NAS_SSH_PORT=5522
NAS_HOST=souchefr.synology.me

# Database Configuration
DB_NAME=rust_app_db
DB_USER=app_user
DB_PASSWORD=TestPassword456!ChangeMe
DB_ADMIN_USER=admin
DB_ADMIN_PASSWORD=TestAdmin456!ChangeMe

# Mongo Express Configuration
MONGO_EXPRESS_USER=test
MONGO_EXPRESS_PASSWORD=test456

# User/Group IDs (Synology)
USER_UID=1026          # Thierry's account on the NAS
USER_GID=100           # Synology "users" group
USERNAME=thierry       # NAS username
GROUPNAME=users

# Logging
RUST_LOG=debug

# Volumes - Absolute paths on Synology NAS
# Note: No Cargo cache needed - executable is pre-compiled
VOLUME_MONGODB_DATA=/volume1/docker/settest/mongodb/data
VOLUME_MONGODB_INIT=/volume1/docker/settest/mongodb/init

# Docker Images (pinned to stable versions)
MONGO_IMAGE=mongo:7.0.15-jammy
MONGO_EXPRESS_IMAGE=mongo-express:1.0.2

# MongoDB Collections
COLLECTION_1=setplayers
COLLECTION_2=setgames
COLLECTION_3=setstats
